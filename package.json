{
  "name": "raksmine",
  "version": "0.1.0",
  "description": "2D mining game in the browser",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
